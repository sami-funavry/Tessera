---
title: "Repo Structure & Scalability"
description: "Directory layout, design rationale, and how to add a new chain or application."
section: 6
slug: "repo-structure"
---

# Repo Structure & Scalability

---

## Directory Layout

```
tessera/
├── contracts-evm/               # Solidity contracts (Foundry)
│   ├── src/
│   │   ├── RelayerRegistry.sol
│   │   ├── Bond.sol
│   │   ├── Verifier.sol
│   │   ├── BridgeVault.sol
│   │   ├── BridgeMint.sol
│   │   ├── TUSDC.sol
│   │   └── interfaces/IApp.sol
│   ├── test/                    # Foundry test files (*.t.sol)
│   └── script/Deploy.s.sol
│
├── contracts-cosmwasm/          # Rust + CosmWasm contracts
│   ├── contracts/
│   │   ├── relayer-registry/
│   │   ├── bond/
│   │   ├── verifier/
│   │   ├── bridge-vault/
│   │   ├── bridge-mint/
│   │   └── tusdc/
│   └── packages/
│       ├── tessera-types/       # shared message types
│       └── tessera-proof/       # shared proof-walking logic
│
├── relayer/                     # Go service
│   ├── cmd/relayer/main.go
│   ├── internal/
│   │   ├── chain/plugin.go      # ChainPlugin interface
│   │   ├── relayer/             # submitter, challenger, runner
│   │   ├── transform/           # Patricia↔IAVL transformation
│   │   └── supabase/            # state persistence
│   └── plugins/
│       ├── ethereum/plugin.go   # EthereumPlugin
│       └── tendermint/plugin.go # TendermintPlugin
│
├── frontend/                    # Next.js 14 App Router
│   ├── app/                     # routes (bridge, demo, dashboard, docs, benchmark)
│   ├── components/              # shared UI components
│   └── lib/                     # wagmi, keplr, supabase clients
│
├── scripts/
│   ├── addresses.json           # deployed contract addresses (source of truth)
│   ├── deploy-evm.sh
│   ├── deploy-cosmwasm.sh
│   └── register-*-relayers.sh
│
├── docs/                        # in-app documentation (this directory)
└── supabase/                    # schema migrations
```

---

## Why This Layout

**One contract set, multiple chains.**
`contracts-evm/src/` contains six `.sol` files. The same compiled bytecode deploys on Sepolia today and on any other EVM chain tomorrow. The only change is deployment configuration. Same principle applies to `contracts-cosmwasm/` — same Rust code, new addresses.

**Plugin isolation.**
Each chain plugin lives in `relayer/plugins/<chain-name>/plugin.go`. The plugin has one responsibility: implement `ChainPlugin`. No other file in the relayer knows the difference between Ethereum and Tendermint at the implementation level.

**Shared packages for correctness.**
`packages/tessera-types/` and `packages/tessera-proof/` are used by all CosmWasm contracts. This ensures the proof-walking logic and message type definitions are consistent across all destination apps — a single fix propagates to all.

---

## Adding a New Source Chain

**What changes:** one new file.

```
relayer/plugins/polygon/plugin.go   ← new file
```

That file implements `ChainPlugin` for the new chain. The relayer runner picks it up via configuration:

```yaml
chains:
  - name: polygon
    plugin: polygon    # ← matches the plugin registration key
    rpc:
      primary: <RPC>
    chain_id: 137
    contracts:
      verifier: <DEPLOYED_VERIFIER>
      ...
```

**What does not change:**
- No existing plugin files.
- No `internal/` files.
- No contract code.
- No frontend code (new chain appears in the chain selector automatically if the frontend reads from config).

---

## Adding a New Destination VM

**What changes:** one new directory of contracts, ported to the new VM's language.

```
contracts-<new-vm>/
├── relayer-registry/
├── bond/
├── verifier/
├── bridge-vault/
├── bridge-mint/
└── tusdc/
```

The six contracts implement the same logical interface. The Verifier dispatches to `IApp`-equivalent in the new VM's style. Existing contracts on Sepolia and Neutron are unchanged.

---

## Adding a New Application

**What changes:** one new contract.

```solidity
// MyApp.sol
contract MyApp is IApp {
    address public immutable verifier;

    modifier onlyVerifier() {
        require(msg.sender == verifier, "not verifier");
        _;
    }

    function onCrossChainMessage(
        bytes32 sourceChainId,
        bytes calldata sourceApp,
        bytes4 action,
        bytes calldata payload
    ) external onlyVerifier {
        // custom application logic
    }
}
```

Register `MyApp`'s address as the `destinationApp` in the message envelope. The Verifier dispatches to it after proof verification. No Verifier changes. No relayer changes. No registry changes.

---

## Contract Addresses (Deployed)

| Contract | Sepolia | Neutron (pion-1) |
|----------|---------|-----------------|
| tUSDC | `0x7dcA285EFe722EdC1D9c93C3878fb58b255EC5B0` | `neutron16ket7npnkekn76nzhfjauwkwsea49rssp9fkn7fyxu35fwavfrxqxp5qnz` |
| Bond | `0x8c7dc28559B75AF8c3d59B62C87309E65cb37912` | `neutron1nnz9j6c3d25wnwj4h3jqkvazgawcmgjjk5unysvf6e0j90gavvsseunvg8` |
| RelayerRegistry | `0x43677d5Da5701E061Eefa65e36A4fF6D4BFC1109` | `neutron1jq5kku3r0sxdkcxvkx7ke4dlcwq4my0m2gncrx4zf7g37hxtwj7qfrya5k` |
| Verifier | `0x2EfAB8cC7ed7C11cfC23C215731aaFA2A602F72a` | `neutron1sda4ucdq06de7h7lxg66n6sq29ft9hk76a5mpjwehk3a8wfga0eqf002f0` |
| BridgeVault | `0x2C3544434185DD65F058494816bB816e5314a29E` | `neutron12z7xqgwgp6vsk5s96z4n6vjupqjg3zmvv5v068vvy3n69gshvhaq8j7dam` |
| BridgeMint | `0x61cab20856b16003b6a3FB213F86355515AD43cd` | `neutron18am0spqaanz75mh2tl43ychhvf537wcklf3rjlv0y03tvrn6gdksq8ltt7` |

Source of truth: [`scripts/addresses.json`](../scripts/addresses.json)
